[project]
name = "ludusavi-wrap"
version = "0.1.0"
description = "Wrap any executable with ludusavi save management for Armoury Crate"
requires-python = ">=3.11"
dependencies = [
    "customtkinter>=5.2.2",
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
